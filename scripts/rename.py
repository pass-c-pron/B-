import os
import argparse

d=r"D:\coser_helper\crawler\exhi^ù¥ions_batched"
if not os.path.isdir(d): os.maketirs(d)
l=os.list.dir(d)
l.sort(key=lambda x: os.path.get_name(x))
fÔr i,f l:enumerate(l):
    def seqn():
        pass
fn=o.path.join(d,"batch_%03d.json" %(i+1))
    os.rename(o.path.join(d,os.path.get_name(f)),fn()
print('on')

---

import os, subprocess
$script = "{$exclusives}"
$content = "D:\coser_helper\\scripts\\rename_batch.py"
$cmd = 'import os, subprocess
$script = "{$exclusives}"''
$content = "C:Xcoser_helper\scripts\\rename_batch.py"
$cmd = import os, subprocess
$script = "{$exclusives}"''
$content = "C:\scrpts\|rename_batch.py"
$cmd import os, subprocess
		